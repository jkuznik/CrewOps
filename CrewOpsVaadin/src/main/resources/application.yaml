server:
  port: 8081

spring:
  application:
    name: CrewOps UI

  devtools:
    restart:
      enabled: false

  profiles:
    active: dev

security:
  properties:
    clientIdInput: ${CREWOPS_CLIENT_ID_INPUT}

core:
  base-url: http://${CREWOPS_CORE_URL}:8080

cache:
  url: ${CREWOPS_CACHE_URL}