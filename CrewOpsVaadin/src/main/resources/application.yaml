server:
  port: 8081

spring:
  application:
    name: CrewOps UI

  profiles:
    active: dev

  cache:
    cache-names: getAllEmployees, getAllQualifications, getCompanyById, getEmployeeById
    redis:
      time-to-live: 3600000

security:
  properties:
    clientIdInput: ${CREWOPS_CLIENT_ID_INPUT}

core:
  base-url: http://${CREWOPS_CORE_URL}:8080

cache:
  url: ${CREWOPS_CACHE_URL}
