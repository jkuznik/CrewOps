name: Run Vaadin container on host

on:
  workflow_run:
    workflows: ["Build And Push vaadin image"]
    types:
      - completed

env:
  AWS_REGION: eu-north-1
  ECR_REPOSITORY: jkuznik-ecr/crewops-vaadin
  ECS_SERVICE: crewops-vaadin-service
  ECS_CLUSTER: crewopc-basic-cluster
  ECS_TASK_DEFINITION: crewops-vaadin

  CONTAINER_NAME: crewops-vaadin-container

permissions:
  contents: read

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build, tag, and push image to Amazon ECR
        id: build-image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          # Build a docker container and
          # push it to ECR so that it can
          # be deployed to ECS.
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT

      - name: Fill in the new image ID in the Amazon ECS task definition
        id: task-def
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: ${{ env.ECS_TASK_DEFINITION }}
          container-name: ${{ env.CONTAINER_NAME }}
          image: ${{ steps.build-image.outputs.image }}

      - name: Deploy Amazon ECS task definition
        uses: aws-actions/amazon-ecs-deploy-task-definition@v1
        with:
          task-definition: ${{ steps.task-def.outputs.task-definition }}
          service: ${{ env.ECS_SERVICE }}
          cluster: ${{ env.ECS_CLUSTER }}
          wait-for-service-stability: true

#     paste this directly under on: workflow_run: [] types: -completed    based on self-hosted runner
#jobs:
#  deploy-vaadin:
#    runs-on: [self-hosted, linux]
#    steps:
#      - name: Pull latest Vaadin image
#        run: sudo docker pull jkuznik/crewops-vaadin:latest
#
#      - name: Authenticate Docker to ECR
#        env:
#          AWS_REGION: ${{ secrets.AWS_REGION }}
#        run: |
#          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
#          ECR_URI="$ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/jkuznik-ecr/crewops-vaadin"
#          aws ecr get-login-password | docker login --username AWS --password-stdin $ECR_URI
#
#      - name: Pull latest image from ECR
#        env:
#          AWS_REGION: ${{ secrets.AWS_REGION }}
#        run: |
#          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
#          ECR_URI="$ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/jkuznik-ecr/crewops-vaadin"
#          sudo -E docker pull $ECR_URI:latest
#
#      - name: Remove old Vaadin container
#        run: sudo docker rm -f CrewOpsVaadin || true
#
#      - name: Run new Vaadin container
#        env:
#          AWS_REGION: ${{ secrets.AWS_REGION }}
#          CREWOPS_CLIENT_ID_INPUT: ${{ secrets.CREWOPS_CLIENT_ID_INPUT }}
#          CREWOPS_CLIENT_ID_HASH: ${{ secrets.CREWOPS_CLIENT_ID_HASH }}
#          CREWOPS_CORE_URL: ${{ secrets.CREWOPS_CORE_URL }}
#          CREWOPS_CACHE_URL: ${{ secrets.CREWOPS_CACHE_URL }}
#          JWT_CREWOPS: ${{ secrets.JWT_CREWOPS }}
#          JWT_EXPIRATION_CREWOPS: ${{ secrets.JWT_EXPIRATION_CREWOPS }}
#        run: |
#          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
#          ECR_URI="$ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/jkuznik-ecr/crewops-vaadin"
#
#          sudo -E docker run -d \
#            --name CrewOpsVaadin \
#            --network crewOps \
#            --restart always \
#            -e CREWOPS_CLIENT_ID_INPUT=$CREWOPS_CLIENT_ID_INPUT \
#            -e CREWOPS_CLIENT_ID_HASH=$CREWOPS_CLIENT_ID_HASH \
#            -e CREWOPS_CORE_URL=$CREWOPS_CORE_URL \
#            -e CREWOPS_CACHE_URL=$CREWOPS_CACHE_URL \
#            -e JWT_CREWOPS=$JWT_CREWOPS \
#            -e JWT_EXPIRATION_CREWOPS=$JWT_EXPIRATION_CREWOPS \
#            -e TZ=Europe/Warsaw \
#            -p 8081:8081 \
#            --memory=2048m \
#            $ECR_URI:latest