#logging:
#  level:
#    #    Show bind information for each query generated by hibernate
#    org.hibernate.orm.jdbc.bind: TRACE

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/${CREWOPS_POSTGRES_DB}
    username: ${CREWOPS_POSTGRES_USER}
    password: ${CREWOPS_POSTGRES_PASSWORD}

  jpa:
    properties:
      hibernate:
#        show_sql: true
#        format_sql: true
