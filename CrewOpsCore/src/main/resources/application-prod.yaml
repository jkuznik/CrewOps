logging:
  level:
#    configure that before first prod instance run
    #    Show bind information for each query generated by hibernate
    org.hibernate.orm.jdbc.bind: TRACE

spring:
  datasource:
    url: jdbc:postgresql://CrewOpsDb:5432/${CREWOPS_POSTGRES_DB}
    username: ${CREWOPS_POSTGRES_USER}
    password: ${CREWOPS_POSTGRES_PASSWORD}