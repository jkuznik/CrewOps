spring:
  liquibase:
    enabled: true
    change-log: db/changelog/db.changelog-test.yaml
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true

security:
  properties:
    jwtExpiration: 10